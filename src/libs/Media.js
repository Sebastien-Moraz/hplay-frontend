export default class Media {
	constructor(id, name, description, price, shareId, preview, tags) {
		this.id = id;
		this.name = name;
		this.description = description;
		this.price = price;
		this.shareId = shareId;
		this.preview = preview;
		this.tags = tags;
	}
}